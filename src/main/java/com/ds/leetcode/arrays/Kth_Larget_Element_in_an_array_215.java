
package com.ds.leetcode.arrays;

public class Kth_Larget_Element_in_an_array_215 {
    
    public int findKthLargest(int[] nums, int k) {
        
        // Since we now all the elements of the array. We can build max heap in O(n).
        // Pop out top element from the heap n-k times.
        // If K> n/2, build min-heap instead of max-heap.
        
        //Total time complexity O(n).
        
        return 0;
        
    }
}
